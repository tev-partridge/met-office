# Weather App Frontend

Deploys a simple site that allows users to input a post code and fetches weather data from the backend. 

## Setup
- Install dependencies with `npm install`
- Make sure the backend is up and running 👍
- Run the program with `npm run dev`
